version: '3'

services:
  haskell:
    container_name: 'haskell_image_gallery'
    build: .
    tty: true
    ports:
      - "3000:3000"
    command: haskell-image-gallery-exe
  #  db:
  #    container_name: 'haskell_image_gallery_db'
  #    image: postgres
  #    restart: always
  #    environment:
  #      POSTGRES_PASSWORD: "12345"
